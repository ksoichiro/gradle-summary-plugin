language: groovy

jdk:
  - openjdk7
  - oraclejdk8

script:
  - ./gradlew clean build check uploadArchives -s
  - cd samples/cobertura; ./gradlew clean check -s; cd ..
  - cd samples/jacoco; ./gradlew clean check -s; cd ..

after_success:
  - ./gradlew cobertura coveralls -s
